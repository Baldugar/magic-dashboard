schema: ../graphql/card_schema.graphqls
generates:
    ./src/graphql/types.ts:
        plugins:
            - typescript
        config:
            namingConvention:
                typeNames: keep
                enumValues: upper-case#upperCase
            scalars:
                Time: Date
