schema: ../graphql/schema.graphqls
generates:
    ./src/graphql/types.ts:
        plugins:
            - typescript
        config:
            sort: false
            namingConvention: keep
            scalars:
                Time: Date
