schema: ../graphql/card_schema.graphqls
generates:
    ./src/graphql/types.ts:
        plugins:
            - typescript
        config:
            namingConvention: keep
            scalars:
                Time: Date
